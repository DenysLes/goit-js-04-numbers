// Написати скрипт перевірки підписки користувача при доступі до контенту
// Є три типи підписки : free, pro, vip
// Отримати доступ можуть лише мкористувачі лише підписок pro та vip

const sub = 'pro'

// якщо підписка pro або vip тоді є доступ до контенту

const canAccessContent = sub === 'pro' || sub === 'vip'  ;
console.log('Є доступ до контенту ? ', canAccessContent);
